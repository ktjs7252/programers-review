나의 코드

#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int solution(int n) {
    int answer = 0;
    int k;
    while(n!=0)
    {
        k=n%10;
        n=n/10;
        answer+=k;
    }


    return answer;
}
